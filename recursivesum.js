/*function digital_root(n) {
    let wArr = [...n+''].map( n => parseInt(n))
    let result =  wArr.reduce((a,b) => a+b,0)
    if (wArr.length ===1 ){
      return result
    } 
    return digital_root(result)
  }*/

